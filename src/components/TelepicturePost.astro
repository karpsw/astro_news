---
// Imports
import Telepicture from './Telepicture.astro';

import type { PostCardItem } from '@/schemas/post-card';

export interface Props {
  post: PostCardItem;
  wImg: number;
  hImg: number;
  imgclass?: string;
}

const { post, wImg = 891, hImg = 501, imgclass = 'aspect-[16/9] max-w-full block' } = Astro.props;
---

{
  post.img && post.img.url && (
    <Telepicture
      src={post.img.url}
      loading="lazy"
      wImg={wImg}
      hImg={hImg}
      imgclass={imgclass}
      posttitle={post.title}
      alt={post.img.alt}
      lightbox="no"
    />
  )
}
