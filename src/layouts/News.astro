---
//import Markdown from 'astro-remote/lib/Markdown.astro';

import { getMarkdown } from '@/modules/post/getArchive';
import { renderMarkdown } from '@/utils/markdown';
import NewsBase from './NewsBase.astro';

import type { BaseHeadProps } from '@/components/BaseHead.astro';
import { formatDate, formatDateIso } from '@/utils/datetime'
import { Icon } from 'astro-icon/components'

const { slug } = Astro.params;

const { data, content } = await getMarkdown(slug!);
const metadata = { metadata: { title: data.title } } as BaseHeadProps;
//const html = await renderMarkdown(rawMd);
const { code: contentHtml } = await renderMarkdown(content);
const { code: lidtHtml } = await renderMarkdown(data.lid ?? '');
 
---

<NewsBase {...metadata}>
  <!-- 📰 Main Content-->
  <main
    class="container grid grid-cols-1 lg:grid-cols-[2fr_1fr] gap-8"
  >
    <!-- 📄 Article Section -->
    <article class="space-y-6">
      <!-- Мета-информация -->
  <div class="text-sm text-gray-500 inline-flex gap-3">
		<span>Общество</span> • 
		 		{data.date && (      
          <Icon name="mdi:access-time" />
          <time itemprop="datePublished" datetime={formatDateIso(data.date)}>
            {formatDate(data.date)}
          </time>        
        )}
	</div>
      <h1 class="text-3xl font-bold text-gray-900 leading-snug">
        {data.title}
      </h1>

      <!-- 🖼️ Image Placeholder -->
      <div class="w-full   bg-gray-200 rounded-md flex items-center justify-center">
       <!-- Картинка -->
  {
    data.featured_img_url && (
      <img
        src={data.featured_img_url}
        alt={data.featured_img_caption}
        loading="eager"
        class="block max-w-full h-auto aspect-[16/8] object-cover border border-base-300"
      />
    )
  }
      </div>

      <!--  Subscribe Block -->
      <div class="bg-blue-50 border border-blue-200 p-4 rounded-md flex items-center space-x-4">
        <div class="w-10 h-10 rounded-full bg-blue-600 flex items-center justify-center">
          <svg
            class="w-5 h-5 text-white"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            viewBox="0 0 24 24"><path d="M4 4h16v16H4z"></path></svg
          >
        </div>
        <div>
          <h2 class="text-blue-700 font-semibold text-sm">Подпишитесь на Telegraf</h2>
          <p class="text-blue-600 text-sm">Новости в Telegram, Viber и других платформах.</p>
        </div>
      </div>

			<div class="border border-blue-200 p-4 rounded-md flex items-center space-x-4" set:html={lidtHtml} />
      <!-- 📝 Article Text -->
      <div class="prose max-w-none prose-blue" set:html={contentHtml} />
      


<section class="bg-white border-t border-gray-200 mt-12 pt-6">
  <div class="max-w-screen-xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-xl font-bold text-gray-900 mb-4">Подобрали для вас</h2>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">

      <!-- 🔹 Карточка 1 -->
      <a href="#" class="block bg-gray-50 border border-gray-200 rounded-md overflow-hidden hover:shadow-sm transition">
        <div class="h-40 bg-gray-200 flex items-center justify-center">
          <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" stroke-width="2"
               viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="14" rx="2" ry="2"/><path d="M3 17l6-6 4 4 8-8"/></svg>
        </div>
        <div class="p-4 space-y-2">
          <h3 class="text-base font-semibold text-gray-800 leading-snug">
            Как постирать в стиралке без воды? На «АТЛАНТе» подсказали способ
          </h3>
          <div class="text-sm text-gray-500">Лайфхаки • 23 июля 2025</div>
        </div>
      </a>

      <!-- 🔹 Карточка 2 -->
      <a href="#" class="block bg-gray-50 border border-gray-200 rounded-md overflow-hidden hover:shadow-sm transition">
        <div class="h-40 bg-gray-200 flex items-center justify-center">
          <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" stroke-width="2"
               viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="14" rx="2" ry="2"/><path d="M3 17l6-6 4 4 8-8"/></svg>
        </div>
        <div class="p-4 space-y-2">
          <h3 class="text-base font-semibold text-gray-800 leading-snug">
            Почему крышечки на стаканчиках со сметаной стало не оторвать?
          </h3>
          <div class="text-sm text-gray-500">Еда • 21 июля 2025</div>
        </div>
      </a>

      <!-- 🔹 Карточка 3 -->
      <a href="#" class="block bg-gray-50 border border-gray-200 rounded-md overflow-hidden hover:shadow-sm transition">
        <div class="h-40 bg-gray-200 flex items-center justify-center">
          <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" stroke-width="2"
               viewBox="0 0 24 24"><rect x="3" y="3" width="18" height="14" rx="2" ry="2"/><path d="M3 17l6-6 4 4 8-8"/></svg>
        </div>
        <div class="p-4 space-y-2">
          <h3 class="text-base font-semibold text-gray-800 leading-snug">
            «Это нормально» — почему последняя минута стирки может длиться 5
          </h3>
          <div class="text-sm text-gray-500">Общество • 22 июля 2025</div>
        </div>
      </a>

    </div>
  </div>
</section>

			</article>
 

    <!-- Sidebar -->
    <aside class="space-y-6">
      <!-- 💱 Currency Block -->
      <div class="bg-white border border-gray-200 p-4 rounded-md">
        <h2 class="text-lg font-semibold text-gray-700 mb-2">Курсы валют</h2>
        <ul class="text-sm text-gray-600 space-y-1">
          <li>USD: 3.25 BYN</li>
          <li>EUR: 3.55 BYN</li>
          <li>RUB: 0.034 BYN</li>
        </ul>
      </div>

			
      <!--   Weather Block -->
      <div class="bg-white border border-gray-200 p-4 rounded-md">
        <h2 class="text-lg font-semibold text-gray-700 mb-2">Погода</h2>
        <p class="text-sm text-gray-600">Бобруйск: +24°C, ясно</p>
        <p class="text-sm text-gray-600">Влажность: 45%, Ветер: 3 м/с</p>
      </div>

      <!--   News Feed -->
      <div class="bg-white border border-gray-200 p-4 rounded-md">
        <h2 class="text-lg font-semibold text-gray-700 mb-2">Последние новости</h2>
        <ul class="text-sm text-gray-600 space-y-2">
          <li>
            <a href="#" class="hover:text-blue-600">Как вернуть носкам белый цвет за 1 рубль</a>
          </li>
          <li><a href="#" class="hover:text-blue-600">Кому не стоит есть помидоры</a></li>
          <li>
            <a href="#" class="hover:text-blue-600">8 продуктов, от которых стоит отказаться</a>
          </li>
        </ul>
      </div>
    </aside>
  </main>
	
	





</NewsBase>
