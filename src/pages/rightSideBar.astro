---
// must be at top
import '@/assets/styles/github-markdown.css';


export const prerender = false;
export const output = "server";
const readmeContent = await fetch('https://telegraf.news/sidebar_ajax1').then((response) => response.text());

---

<div id="content" set:html={readmeContent}></div>
